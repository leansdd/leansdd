{
  "name": "leansdd-monorepo",
  "private": true,
  "version": "0.2.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @leansdd/cli",
    "test": "npm run test -w @leansdd/cli"
  }
}
